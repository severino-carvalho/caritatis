# Arquivo de referência das variáveis de ambiente do frontend.
# Copie para `.env.local` e preencha os valores. Não comite `.env.local`.

# URL base da API do backend (Spring Boot).
# Exemplo de desenvolvimento: http://localhost:8080
VITE_API_BASE_URL=
